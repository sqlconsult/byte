#!/usr/bin/env python3

# define compass points for easy indexing
COMPASS = ['N', 'E', 'S', 'W']

# define grid moves for each compass point
# list elements in grid_moves match compass points
GRID_MOVES = [(0, 1), (1, 0), (0, -1), (-1, 0)]